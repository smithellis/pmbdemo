<%= form_for(@salesgroup) do |f| %>
  <% if @salesgroup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@salesgroup.errors.count, "error") %> prohibited this salesgroup from being saved:</h2>

      <ul>
      <% @salesgroup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
